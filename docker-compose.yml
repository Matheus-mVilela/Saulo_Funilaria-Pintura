version: '3.7'

services:
    app:
        build: proxy
        restart: ${RESTART:-no}
        ports: 
            - "${HOST_PORT}80"
        volumes: 
            - ./app:/app
        environment:
            VIRTUAL_HOST: srfunilaria.com
            VIRTUAL_HOST_ALIAS: www.srfunilaria.com,srfunilaria.com
            LETSENCRYPT_HOST: srfunilaria.com
            LETSENCRYPT_EMAIL: matheus_vilela@icloud.com
        networks:
            - nginx-gateway

networks:
    nginx-gateway:
        external:
            name: nginx-gateway